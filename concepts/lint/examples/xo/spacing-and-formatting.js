// Example 4: Spacing and formatting issues
const obj = { key: 'value', another: 'test' };
const arr = [1, 2, 3, 4];

if (condition) {
  doSomething();
} else {
  doSomethingElse();
}

for (let i = 0; i < 10; i++) {
  console.log(i);
}

// Object spacing
const formatted = { key: 'value', another: 'test' };
const nested = { outer: { inner: { deep: 'value' } } };

// Function calls with inconsistent spacing
fn(arg1, arg2);
another(arg1, arg2);
third(arg1, arg2);

// Operators
const sum = 5 + 10;
const product = 5 * 10;
const comparison = x > y;
const logical = a && b || c;

// Semicolons
const noSemi = 'missing';
const hasSemi = 'present';
